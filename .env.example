
# Required
S3_BUCKET=your-bucket-name
AWS_REGION=us-east-1

# One of the following auth methods:
# 1) Instance profile/role (preferred in cloud) – set nothing here.
# 2) Static keys (dev/local only):
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...

# Optional S3-compatible endpoint
# S3_ENDPOINT_URL=https://s3.your-provider.example

# Cache directory (inside containers: use tmpfs or ephemeral disk)
CACHE_DIR=/tmp/imgcache

# Manifest path in the bucket
MANIFEST_KEY=manifests/inventory-v1.jsonl

# Optional: CSV mapping (filename,logical_id)
# DATASET_LOGICAL_MAP=./logical-map.csv
